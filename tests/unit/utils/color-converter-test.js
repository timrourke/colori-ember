import colorConverter from 'color-storm/utils/color-converter';
import { module, test } from 'qunit';

module('Unit | Utility | color converter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colorConverter();
  assert.ok(result);
});
